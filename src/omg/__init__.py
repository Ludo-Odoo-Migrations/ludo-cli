"""omg — transport-only CLI client for LUDO Odoo migrations.

This package contains no engine code: it talks to a LUDO deployment over the
public Contract A (REST) + Contract B (events) surface. See README.md.
"""

__version__ = "0.0.1"
